load("//rules:defs.bzl", "write_go_proto_srcs")

write_go_proto_srcs(
    name = "update_protos",
    additional_update_targets = [
        "//pkg/foo:update_protos",
    ],
)

exports_files(["MODULE.bazel"], visibility = ["//visibility:public"])
